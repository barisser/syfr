from setuptools import setup

setup(
      name='syfr',
      version='0.0.1',
      description='Always Open Always Safe',
      url='syfr.io',
      license='MIT',
      install_requires=[
            'cryptography'
        ]
      )
